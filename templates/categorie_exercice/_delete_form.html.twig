<form method="post" action="{{ path('categorie_exercice_delete', {'id': categorie_exercice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_exercice.id) }}">
    <button class="btn">Delete</button>
</form>
